{
  "name": "カロナビ (AYG) — Flutter",
  "install": "flutter pub get",
  "terminals": [
    {
      "name": "web-preview",
      "command": "flutter run -d web-server --web-hostname 0.0.0.0 --web-port 8080 --target lib/main_web.dart"
    }
  ],
  "ports": [8080]
}
